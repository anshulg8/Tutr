# This file should contain all the record creation needed to seed the database with its default values.
# The data can then be loaded with the rake db:seed (or created alongside the db with db:setup).
#
# Examples:
#
#   cities = City.create([{ name: 'Chicago' }, { name: 'Copenhagen' }])
#   Mayor.create(name: 'Emanuel', city: cities.first)

Post.create(title: 'test_post', content: 'Hello World: Our first program will print the classic “hello world” message. Here’s the full source code.

```python
print ("Hello, World!")
```
Wait! What? Is that it? You bet it is. Congrats, You just wrote your first python program.

If you are running older version of Python (Python 2.x), use of parenthesis as in print function is optional.

To run the program, put the code in hello-world.py and type python hello-world.py in terminal. This produces the following result:

**Screenshot**

Now that we can run and build basic Python programs, let’s learn more about the language.')